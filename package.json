{
  "name": "erp-3-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace @erp/api",
    "dev:web": "npm run dev --workspace @erp/web",
    "build": "npm run build --workspace @erp/database && npm run build --workspace @erp/api && npm run build --workspace @erp/web",
    "lint": "npm run lint --workspace @erp/api && npm run lint --workspace @erp/web",
    "db:generate": "npm run db:generate --workspace @erp/database",
    "db:push": "npm run db:push --workspace @erp/database",
    "db:seed": "npm run db:seed --workspace @erp/database"
  }
}
